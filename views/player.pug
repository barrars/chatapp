#slide-out.player.sidenav
  input#find.input(placeholder="search" type="text", name="find")
  #player
    //- i.fas.fa-volume-down
    //- #volume
    //- .ico
    //-   i.fas.fa-volume-up
    //- .time
  #songList
    - if (songs){
      each val, i in songs
        div.card.song(data-name = val data-order = i)
          p.inline(data-name = val)=val
          i.hidden.fas.fa-pen.editIcon(data-name=val title="edit title")
          i.add_song.hidden.fas.fa-plus(data-name=val title="copy link")
          i.fas.hidden.fa-trash-alt(data-name=val title="{delete song}")
      - }
  audio#audio-element(muted  src="")
  //- input#rename

